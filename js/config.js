// Configuration settings
const CONFIG = {
    // API endpoints
    API_URL: 'https://your-api-endpoint.com',
    
    // Cache settings
    CACHE_DURATION: 3600000, // 1 hour in milliseconds
    
    // Image settings
    DEFAULT_IMAGE: 'images/default-profile.jpg',
    
    // Search settings
    MIN_SEARCH_CHARS: 2,
    SEARCH_DEBOUNCE_MS: 300,
    
    // Section IDs
    SECTIONS: {
        MILITARY: 'military',
        POLICE: 'police',
        GOVERNMENT: 'government',
        OTHER: 'other'
    }
};
